#what is a linked list?

#A Linked List is a sequence of data elements, which are connected 
# together via links (or pointers). Unlike a Python list (array), 
# where elements are stored in contiguous memory locations, 
# a linked list is dynamic and scattered.

